# realpro（物件費用見積書アプリ）マスタードキュメント
作成日：2026-05-26　作成者：山田謙蔵 × Claude

> このファイル1つで「このアプリは何か・何を使っているか・どこまで作ったか」がわかるようにまとめたもの。

---

## 目次

1. アプリの概要・目的
2. プロジェクト基本情報
3. 登録済みサービス一覧
4. Vercel 環境変数一覧
5. 実装済み機能
6. ファイル構成
7. ユーザー管理
8. 今後やりたいこと
9. 注意事項
10. ハマりポイント / 教訓
11. 開発の歴史

---

## 1. アプリの概要・目的

お客様に賃貸物件の初期費用と「ここでの暮らし」をわかりやすく説明するための**不動産業者向けWebアプリ**。

- 物件の契約書・条件表のスクリーンショットをアップロードすると、Claude AIが自動で費用を読み取る
- 10言語対応のPDFを生成して、LINEやその他の方法でお客様に共有できる
- 担当者コメントもAIが下書きを生成（周辺施設・顧客ヒアリングを反映した「生活のリアルなイメージ」）
- 賃貸仲介を行う業者の「現場ツール」として設計

### 位置づけ

housing-jp（賃貸仲介AIシステム）と連携するツール。housing-jpで内見・契約が決まったお客様に初期費用を説明する際に使う。将来的には他社への月額SaaS販売も検討中。

---

## 2. プロジェクト基本情報

| 項目 | 値 |
|------|------|
| GitHub | https://github.com/yamadakenzo/realpro |
| 本番URL | https://realpro-one.vercel.app |
| ログインURL | https://realpro-one.vercel.app/login |
| ローカルパス | C:\Users\yamad\realpro |
| 技術スタック | Next.js 15 + TypeScript + Tailwind CSS + Anthropic SDK |
| 本番ブランチ | main（Vercel自動デプロイ対象） |
| デプロイ方法 | mainブランチにpushすると即座に本番反映 |
| デザイン | ヘッダー背景：#f7f9f4（アイボリー）/ アクセント：#2d5e3a（フォレストグリーン） |
| 状態 | 本番稼働中。提携先にテスト提供中（2026年5月11日〜） |

---

## 3. 登録済みサービス一覧

> APIキー・パスワードの実値は OneDrive > アプリ開発 > アカウント＆APIキー一覧.txt に保存。

### GitHub

| 項目 | 値 |
|------|------|
| アカウント | yamadakenzo |
| リポジトリ | yamadakenzo/realpro |
| 認証方式 | GitHub CLI（gh）経由のOAuth（Google連携） |
| ⚠️ トークン有効期限 | 2026年6月7日（期限切れ前に更新必要） |

### Vercel

| 項目 | 値 |
|------|------|
| アカウント | yamadakenzo-5126s-projects |
| ダッシュボード | https://vercel.com/yamadakenzo-5126s-projects |
| 本番URL | https://realpro-one.vercel.app |

### Supabase（mitsumori-navi）

| 項目 | 値 |
|------|------|
| プロジェクト名 | mitsumori-navi |
| リージョン | Northeast Asia (Tokyo) |
| Project URL | https://rctfvmsaavohethsxarl.supabase.co |
| ダッシュボード | https://supabase.com/dashboard/project/rctfvmsaavohethsxarl |
| ユーザー管理 | https://supabase.com/dashboard/project/rctfvmsaavohethsxarl/auth/users |
| キー保存場所 | OneDrive → アカウント＆APIキー一覧.txt |
| 備考 | housing-jpの「見積もりURL自動入力」機能でもこのDBを参照している |

### Anthropic API（Claude API）

| 項目 | 値 |
|------|------|
| 用途 | スクリーンショットから費用を自動読み取り（2段階処理）／担当者コメントのAI下書き生成 |
| キー保存場所 | OneDrive → アカウント＆APIキー一覧.txt |
| 備考 | housing-jpと同じキーを共用。解析ボタン・AI生成ボタン押下のたびに課金発生 |

### Google Maps Platform（Places API + Geocoding API）

| 項目 | 値 |
|------|------|
| 用途 | 物件住所から緯度経度→周辺施設（駅・スーパー・コンビニ・学校）取得。AIコメント生成で使用 |
| 必要API | Geocoding API ／ Places API（Nearby Search） |
| 取得方法 | Google Cloud Console → APIとサービス → 認証情報 → APIキー作成 |
| キー保存場所 | OneDrive → アカウント＆APIキー一覧.txt（追記予定） |
| 備考 | 月額無料枠あり。キー未設定時はAI生成が周辺施設なしで動作（コメントは生成可能） |

---

## 4. Vercel 環境変数一覧

> 実値はOneDriveに保存。設定はVercelダッシュボードから。

| 変数名 | 値のポイント |
|--------|------------|
| NEXTAUTH_URL | https://realpro-one.vercel.app（本番） / http://localhost:3000（ローカル） |
| NEXTAUTH_SECRET | mitsumori-navi-secret-key-2026-yamada-realpro |
| NEXT_PUBLIC_SUPABASE_URL | https://rctfvmsaavohethsxarl.supabase.co |
| NEXT_PUBLIC_SUPABASE_ANON_KEY | Supabase Publishable Key |
| SUPABASE_SERVICE_ROLE_KEY | Supabase Secret Key |
| ANTHROPIC_API_KEY | Claude APIキー |
| GOOGLE_PLACES_API_KEY | Google Maps Platform APIキー（Geocoding + Places Nearby Search 有効化） |

---

## 5. 実装済み機能

### 画像読み取り・費用計算

- スクリーンショット複数枚アップロード・削除・リセット
- Claude APIによる2段階読み取り（精度向上のため）
- 仲介手数料の自動計算（家賃 × 1.1、管理費除く）
- 費用一覧の表示・手動編集
- 費用項目の追加・削除
- 不足項目アラート機能

### PDF出力

- PDF出力ボタン押下後に言語を選択する方式
- **対応言語（10言語）：** 日本語・英語・中国語（簡体）・中国語（繁体）・韓国語・ベトナム語・ネパール語・スペイン語・ポルトガル語・インドネシア語
- 費用項目名を日本語と翻訳で常に併記（例：敷金（Security Deposit））
- ふりがな・用語解説の追記
- 住所の英語表記自動生成
- 物件写真の複数枚添付（未添付時はPDFに非表示）
- 月額費用セクション
- 免責文言（仲介業者向け・お客様向けの2種類）

### 担当者・会社情報

- 担当者情報・会社ロゴ入力（プレースホルダー化：未入力時はPDF・テキストに反映しない）
- 会社名・ロゴのPDF自動反映
- お客様へのメッセージ欄
- **担当者コメントAI自動生成（2026-05-26追加 → 同日改善）**
  - 言語選択（10言語）→「AIで自動生成」ボタンで日本語＋選択言語の併記コメントを生成
  - Google Places APIで物件住所から周辺施設を取得（駅・バス停・スーパー・コンビニ・ドラッグストア・100均・内科・歯科・公園・保育園/幼稚園/小学校・コインランドリー・郵便局・ATM）。各施設は徒歩分数（80m/分換算の概算）も付記
  - URLパラメータ `?customerId=xxx` でhousing-jpの顧客ヒアリング結果（Supabase `customers` テーブル）と連携してパーソナライズ
    - 駐車場希望・子育て重視・ペット・自転車などのキーワードを自動検出し、強調すべき施設を変える
    - 駅が遠い物件（徒歩15分以上）の場合はバス停情報や近隣の生活施設を補強
  - コメント構成：①セールスポイント ②交通アクセス ③生活施設 ④担当者の一言（3〜4文）
  - 「外国人向け」「はじめて日本で」などの表現は使わず、生活のリアルなイメージが伝わる文章を生成
  - 生成結果はそのままtextareaで手動編集可能（あくまで下書き）

### 共有・管理

- **LINE共有：** ボタン押下後にPDF/テキスト選択モーダルを表示
- 見積もり保存・履歴タブ
- 比較表タブ（複数物件の並べて比較）
- 見積もりのリセット

### ログイン・認証

- メールアドレス＋パスワードでログイン（NextAuth.js）
- 未ログイン時は /login にリダイレクト
- Supabaseによるユーザー管理

---

## 6. ファイル構成

```
C:\Users\yamad\realpro\
└── src/
    ├── app/
    │   ├── layout.tsx                      ルートレイアウト
    │   ├── page.tsx                        メインページ（全機能の中心）
    │   ├── globals.css                     グローバルCSS・カラー変数
    │   ├── login/
    │   │   └── page.tsx                   ログインページ
    │   └── api/
    │       ├── analyze/route.ts                 画像解析APIエンドポイント
    │       ├── generate-comment/route.ts        担当者コメントAI生成（Google Places + Claude）
    │       └── auth/[...nextauth]/route.ts      NextAuth APIルート
    ├── components/
    │   ├── ImageUploader.tsx              画像アップロード
    │   ├── AgentInfoForm.tsx              担当者情報フォーム
    │   ├── CustomerInfoForm.tsx           お客様情報フォーム
    │   ├── CostTable.tsx                  費用一覧テーブル（PDF描画本体）
    │   ├── PdfExportButton.tsx            PDF出力ボタン
    │   ├── PdfModal.tsx                   PDFオプションモーダル
    │   ├── EstimateManager.tsx            見積もり管理
    │   ├── HistoryTab.tsx                 保存済み履歴タブ
    │   ├── CompareTab.tsx                 比較表タブ
    │   └── LogoutButton.tsx              ログアウトボタン
    ├── lib/
    │   ├── translations.ts               多言語定義（10言語）
    │   └── supabase.ts                   Supabaseクライアント
    ├── auth.ts                            NextAuth設定本体
    └── proxy.ts                           ルート保護（ミドルウェア）
```

---

## 7. ユーザー管理

### アカウント作成手順

1. Supabase ダッシュボード → Authentication → Users
2. 右上「Add user」→「Create new user」
3. Email・Passwordを入力し「Auto Confirm User」にチェック
4. 「Create user」をクリック

### 登録済みユーザー

| メールアドレス | 備考 |
|----------------|------|
| yamadakenzo@gmail.com | 開発者アカウント |
| akeitoa080626@gmail.com | テスト提供先（2026年5月11日〜） |

---

## 8. 今後やりたいこと

### Phase 1（テスト提供→フィードバック→改善）

- [ ] テスト提供先（akeitoa080626@gmail.com）からのフィードバック収集・反映
- [ ] アプリの正式名称を決める
- [ ] 独自ドメインの取得・Vercelへの紐付け
- [ ] UIデザインの刷新（shadcn/ui）
- [ ] ログイン後に会社名・ロゴ・担当者情報が自動セットされる仕組み

### Phase 2（商品化に向けて）

- [ ] 物件・顧客の保存管理
- [ ] パスワードリセット機能
- [ ] 料金プランの設計・Stripe決済連携
- [ ] API使用回数制限の実装

### Phase 3（提案資料化・差別化）

- [ ] **顧客に送れる多言語の物件比較シート**
  - 写真＋AI担当者コメント入りで、PDF/URL/LINEのいずれかで顧客に直接送れる比較資料
  - 比較対象の物件データは Supabase に `slug` をキーに保存して再利用（housing-jp と共有可能な形に）
  - 多言語化は既存の `translations.ts` を流用、AIコメントは顧客の希望言語で生成
  - housing-jp の LINE 送信フローとの連携も検討（housing-jp で内見した顧客に realpro から比較シートを送る一連の動線）
- [ ] **比較表に「フルのAI担当者コメント」を表示**
  - 現状の「おすすめポイント」（1〜2行の手入力）に加えて、各物件のAI担当者コメント全文を比較表のセルに並べる（上記比較シートの一部）
- [ ] **リアプロ（マイソク）スクショからの物件画像自動抽出**
  - スクショに含まれる小さい外観写真・間取り図を自動で切り出して「物件写真」セクションに反映
  - 難物（OCR＋画像領域検出が必要）。完全自動 or 半自動（候補をユーザーが1クリックで採用）を要設計

### 別ソフトとして検討中のアイデア（このアプリには入れない）

- 入居後ガイドの多言語自動生成
- 家族・送金主向けサマリーPDFの別言語生成
- 契約書スキャン → 重要条項の多言語要約
- お客様マイページ発行（ユニークURLで見積もりを確認）
- 複数物件の統合提案書PDF生成

---

## 9. 注意事項

| 項目 | 内容 |
|------|------|
| Claude APIキー | GitHubに絶対push禁止。使用のたびに課金発生 |
| GitHubトークン | 有効期限：2026年6月7日。期限切れ前に更新が必要 |
| Supabase Secret Key | サーバーサイドのみで使用（クライアントに露出させない） |
| Vercel自動デプロイ | mainブランチにpushすると即座に本番反映される |
| テスト提供先への注意 | 画像解析は1日10〜20回程度を目安にしてもらう |

---

## 10. ハマりポイント / 教訓

過去に詰まった現象と原因・対処法のメモ。同じ現象を再発した時はまずここを見る。

### 【2026-05-29】新ページ（/compare）が本番で404になる ＝ ファイルの git add 漏れ

**現象**
- 前日に実装したはずの `/compare` ページが本番（realpro-one.vercel.app）で404
- ローカルでは動く・マスタードキュメントにも「push済み・動作確認済み」と書いてある
- LINEで顧客に送ったURLが開けない状態になっていた

**原因（切り分け済み）**
- Vercel の webhook 問題ではなかった
- **新規ファイル `src/app/compare/` が一度も `git add` されておらず Untracked のままだった**のが真因
- ドキュメント更新コミットだけが push され、肝心のソースファイルがリモート（main）に存在しなかった
- `proxy.ts` / `CompareTab.tsx` / `translations.ts` の変更も未コミットだった
- → 本番にはページのコード自体が無いので、ルートが存在せず404

**対処**
1. `git status` で **Untracked files（新規ファイル）と modified の取りこぼし**を確認
2. 該当ファイルを `git add` → commit → `git push origin master:main`
3. 念のため `npx vercel deploy --prod --yes` で強制デプロイ（webhook 切れの可能性も同時に潰せる）
4. `curl -s -o /dev/null -w "%{http_code}" "https://realpro-one.vercel.app/compare?s=test"` で **HTTP 200** を確認（404 → 200 になればOK）

**教訓**
- 「ローカルで動くのに本番で404」を見たら、まず `git status` で **新規ファイルが Untracked のまま残っていないか**を真っ先に確認する（webhook を疑うのはその後）
- 新しいページ・コンポーネントを足したコミットでは、`git status` に Untracked files が残っていないか必ず目視する（`git commit -am` は新規ファイルを拾わない点に注意）
- 「ドキュメントに push 済みと書いてある」＝「実際に push された」ではない。ドキュメントとリポジトリの実態は別物として、リモートの状態で判断する

### 【2026-05-28】Vercel の GitHub 自動デプロイが突然効かなくなる

**現象**
- `git push origin master:main` で push は GitHub に正常に届く（コミットがリモートに反映される）
- ローカルでは `npx next build` も通る
- にもかかわらず Vercel の Deployments 画面に新規デプロイが出てこない
- 本番URLにアクセスすると最新ではない古いコード（数日前のコミット）のままになる
- housing-jp でも同じ現象に遭遇したことがあるパターン

**原因（切り分け済み）**
- コード／ビルド設定の問題ではない（手動の `npx vercel deploy --prod` は成功する）
- Vercel 無料プランの制限でもない（実際にCLI経由ではビルド・デプロイともに成功し、エラーメッセージは何も出なかった）
- **Vercel と GitHub を繋いでいる webhook（Git Integration）が黙って切れていた** のが真因

**対処**
1. **応急処置**：`npx vercel deploy --prod` を realpro フォルダで実行 → CLI 経由で本番に直接デプロイ。「Link to existing project?」と聞かれたら既存の `yamadakenzo-5126s-projects/realpro` にリンク（新規作成しない）
2. **恒久対処**：Vercel ダッシュボード → プロジェクト → Settings → Git → Connected Git Repository を **Disconnect → 再接続（GitHub再認可）**
3. **動作確認**：`git commit --allow-empty -m "chore: test auto-deploy"` を push して、Vercel Deployments にコミットハッシュ付きの新デプロイが走るかを見る。空コミットなら本番に何の変化も与えず、純粋にwebhookの生死だけ確認できる

**教訓**
- 「push通ったのにデプロイされない」を見たら、まずVercel Deploymentsを開いて新規デプロイの有無を目視確認する（最新のreadyStateや日時を直接見る）
- CLI デプロイは GitHub Integration を経由しないので、Integration の生死を切り分けるリトマス試験紙として使える
- 切り分け順：①Vercel Deployments に新規が出ているか → ②出ていなければ手動 `vercel deploy --prod` を試す（コード・制限の問題か webhook の問題か切り分け） → ③CLIで成功するなら GitHub Integration の再接続

---

## 11. 開発の歴史

### 2026-05-29（夕方・新機能）

**物件写真：スクショ手動トリミングツールを追加**

マイソク画面の小さな写真を1枚ずつ拡大→スクショ→保存→アップロード…と繰り返す手間を解消するため、
1枚のスクショ上で枠をドラッグして必要部分だけを切り出し、そのまま「物件写真」に追加できる手動トリミングツールを実装した。

- **新規ファイル：`src/components/PhotoCropModal.tsx`**
  - 「Step 2 — 物件写真」の「📸 スクショから切り出す」ボタンで開くモーダル
  - 取り込みは2通り：(a) ファイル選択　(b) クリップボード貼り付け（Ctrl+V）。Win+Shift+S で撮ってそのまま貼れる
  - 画像は `max-height:60vh` で縮小表示。選択範囲は空き領域ドラッグで新規作成、四隅・辺の8ハンドルでサイズ調整、中央ドラッグで移動
  - 「この範囲を追加」：**表示座標を元画像の実ピクセル座標にスケール換算**してから `canvas.drawImage`（ソース矩形指定）→ `toBlob` で切り出し。縮小表示による画質低下を回避
  - 書き出しは **PNG優先**、1.5MB超のときのみ JPEG 0.9 にフォールバック（間取り図など線画も綺麗に）
  - スクショは開いたまま複数枚を連続で切り出せる。「このスクショから N枚追加」のカウンタを表示
  - Escape／背景クリックで閉じる。閉じると一時state（取り込み画像・選択範囲・カウンタ）を破棄し object URL を revoke
- **更新：`src/app/page.tsx`**
  - `cropModalOpen` state と `handleCroppedPhotoAdd(file, url)` を追加
  - **重要：`propertyPhotos` は data URL ではなく `File[]`**（保存/URL共有時に Supabase Storage へアップロード）。
    `propertyPhotoUrls`（object URL）はPDF表示・プレビュー用。切り出し画像は既存の `handlePhotoAdd` と同様に**両方へ追加**するので、既存のPDF添付・URL共有フローにそのまま乗る
  - 既存の「＋」ファイルアップロードはそのまま温存（壊していない）
  - `clearPropertyState` に `setCropModalOpen(false)` を追加。新物件の解析開始／リセット時にモーダルを閉じ、一時stateを残さない（切り出し画像は既存ロジックで両配列クリア＋URL revoke 済み）

**変更ファイル：**
- 新規：`src/components/PhotoCropModal.tsx`
- 更新：`src/app/page.tsx`（import・state・ハンドラ・ボタン・モーダル描画・clearPropertyState）

**触っていないもの：** ImageUploader / CostTable / PDF / analyze / generate-comment / 保存・履歴・比較表（propertyPhotos に流すだけで完結）

**SQL変更：** 不要。

**動作確認：** `npx tsc --noEmit` / `npx next build` 共にエラーなし。本番（main）へのpushは見た目確認後に判断（未push）。

---

### 2026-05-29

**比較表のURL共有機能（多言語対応）を追加**

複数物件を1つのURLで顧客に送れるようにした。housing-jp 側で顧客の言語が分かるので、`?lang=` で初期言語をプリセットできる動線も用意。

- **新ページ：`/compare?s=slug1,slug2,slug3&lang=ja`（最大5物件）**
  - URLパラメータの slug は estimates テーブル（Supabase）の slug（8文字ランダム）
  - `?lang=ja` で初期言語指定。未指定なら ja。ページ上部で10言語切り替え可能
  - ログイン不要（顧客向けページ）。`src/proxy.ts` の `PUBLIC_PATHS` に `/compare` を追加して認証除外
  - 認証除外パス：`["/login", "/estimate", "/compare"]`
  - 共有URLの順番を尊重して並べる（Supabaseは `IN` 検索なので順序不定→クライアント側で並べ直し）
  - 期限切れ（`expires_at`）はフィルタで除外
  - 全件取れなかった（or 期限切れ）場合は notFound 画面
- **新規ファイル：**
  - `src/app/compare/page.tsx`（サーバーコンポーネント：URLパラメータパース → Supabase fetch → CompareView に流す）
  - `src/app/compare/CompareView.tsx`（クライアント：lang state ＋比較表本体）
- **CompareView の構成：**
  - 物件情報セクション：住所・部屋番号・間取り・面積・築年数・最寄り駅（徒歩X分）・設備・おすすめポイント
  - 周辺施設セクション（少なくとも1物件に nearby データがあれば表示）：スーパー／コンビニ／ドラッグストア／公園／小学校
  - 月額：家賃／管理費／月額合計
  - 初期費用：8項目＋合計（最安ハイライト）
  - 縦長は `whitespace-pre-wrap`＋`align-top`、スマホは `overflow-x-auto`
  - 項目列を `sticky left-0` にして横スクロール時も常に見える
- **多言語対応：**
  - `src/lib/translations.ts` の `T` に比較表用キーを10言語分追加
    - `compareTitle / propertyN / address / buildingAge / nearestStation / facilities / recommendPoint / nearbyFacilities / supermarket / convenienceStore / drugstore / park / elementarySchool / monthlyPayment / walkMinutesPrefix / walkMinutesSuffix / selectLanguage / loadingCompare / notFoundCompare / copyCompareUrl / copiedCompareUrl / poweredBy`
  - 既存の `COST_LABELS` / `MONTHLY_LABELS`（多言語マップ）を直接使って費目ラベルを翻訳
  - 「徒歩X分」は言語ごとに前置詞／後置詞を切替（例：en は `5 min walk`、ja は `徒歩5分`）
- **CompareTab に「比較表URLをコピー」ボタン追加：**
  - 2件以上選択時に表示
  - クリックで各選択物件を `/api/estimates` に POST → Supabase に保存して8文字 slug を取得 → `https://realpro-one.vercel.app/compare?s=slug1,slug2` を組み立ててクリップボードへ
  - `lang` パラメータは付けない（顧客側で切替）
  - 「コピーしました！」を2秒表示。失敗時は赤字でエラー文言3秒表示
  - 既存の `agentInfo / customerInfo / comment / nearby` も payload に含めて送る（`nearby` を含めれば共有先でも周辺施設行が表示される）
- **データ構造：**
  - 新テーブル不要。既存 `estimates` テーブル（`slug` ＋ `data` JSONB）をそのまま流用
  - `SavedEstimate.slug`（物件名+住所+部屋番号 のローカル重複防止キー）と Supabase の `estimates.slug`（8文字ランダム）は別物。前者は localStorage 内、後者は共有URL用

**変更ファイル：**
- 新規：`src/app/compare/page.tsx`
- 新規：`src/app/compare/CompareView.tsx`
- 更新：`src/lib/translations.ts`（10言語に比較表用キー追加）
- 更新：`src/proxy.ts`（PUBLIC_PATHS に `/compare` を追加）
- 更新：`src/components/CompareTab.tsx`（URLコピーボタンとロジック追加）

**SQL変更：** 不要。

**動作確認：** `npx tsc --noEmit` / `npx next build` 共にエラーなし。`/compare` ルートはサーバーレンダリング（ƒ）として認識。

**スコープ外（やっていないこと）：**
- 比較表のPDF出力

---

### 2026-05-29（午後・緊急修正）

**/compare が本番で404だったのを修正＋housing-jp の LINE 送信に一言追加**

- **404の原因と修正：** `src/app/compare/` 一式が `git add` されておらず本番にコードが無かった（詳細は「10. ハマりポイント」の【2026-05-29】参照）。未追跡だった compare ページと未コミットだった `proxy.ts` / `CompareTab.tsx` / `translations.ts` をコミットし `git push origin master:main` → `npx vercel deploy --prod --yes` で強制デプロイ → `/compare?s=test` が **HTTP 200** になったことを確認
- **housing-jp 側：比較表URLのLINE送信に顧客言語のあいさつ文を追加（前回スコープ外だった連携の一部を実施）**
  - URLだけ送ると顧客が不審に思うため、「ご提案物件の比較表です。ご確認ください。」のような一言を先頭に付ける
  - 汎用の `send-line` API は触らず、`housing-jp/app/admin/page.tsx` の送信テキスト組み立て側（`buildCompareMessage`）で対応
  - realpro の `translations.ts` と同じ言語コード（ja/en/zh/zh-tw/ko/vi/ne/es/pt/id）で10言語分のあいさつ文を用意。日本語以外は現地語＋日本語を併記。未対応コードは ja にフォールバック
  - **変更ファイル：** `housing-jp/app/admin/page.tsx`（`COMPARE_GREETINGS` / `buildCompareMessage` 追加、`sendCompare` がそれを使うよう変更）

**動作確認：** realpro・housing-jp ともに `npx tsc --noEmit` / `npx next build` エラーなし。realpro は本番デプロイ済み・`/compare` 200確認済み。

---

### 2026-05-28（午後・不具合修正）

**比較表充実機能の運用不具合を修正**

c74d01eで入れた多面比較機能を本番で使ったところ、3つの不具合が判明したので修正した。共通の根っこは「新しい画像を解析するときに前物件の依存stateが消えていない」ことだった。

- **症状3（根っこ・最優先）**：別物件の解析画面に前物件の担当者コメントが残る → `handleAnalyze` が `setResult(null)` しかしておらず、`comment` / `nearby` / `customerInfo` / `propertyPhotos` / `validUntil` / `saveName` などをリセットしていなかった
  - **修正**：`clearPropertyState()` ヘルパを切り出し、`handleAnalyze` と `handleReset` から共通利用するよう統一。新解析開始時点で前物件の依存stateを必ず初期化
- **症状2**：「保存」→「比較表に追加」で同じ物件が2件登録される → `Date.now()` ベースの新規IDで毎回追加していたため重複していた
  - **修正**：`SavedEstimate.slug`（物件名+住所+部屋番号）を追加。同じslugの保存先があれば既存entryを上書き、なければ新規追加。`handleAddToCompare` も同じ `handleSave` を経由するので二重押しでも1件に収束する
  - **UI整理**：「比較表に追加」ボタンを「保存して比較表へ」に改名。さらに下に「同じ物件なら上書きされて重複しません」と明示
- **症状1**：本番で新項目の入力UI・比較表の新行が出ない → コードは既に対応済み（`物件詳細`セクション・CompareTabの新行・`|| "—"` フォールバックすべて実装済み）。**Vercel側のビルドキャッシュかデプロイ未反映**が原因と推定。本修正をmainにpushすれば再デプロイで反映されるはず

**変更ファイル：**
- 更新：`src/types/index.ts`（`SavedEstimate.slug?` を追加）
- 更新：`src/app/page.tsx`（`clearPropertyState` ヘルパ追加・`handleSave` をslugベースのdedup付きに変更・「比較表に追加」ボタンの文言と説明文を整理）

**SQL変更：** 不要（localStorage / Supabase の `data` JSONB に格納するため、slug は新規キー追加だけで済む）

**動作確認：** `npx tsc --noEmit` / `npx next build` 共にエラーなし。

---

### 2026-05-28

**比較表タブを多面比較化（価格以外も並べて見られるように）**

営業現場で複数物件を並べて見せるとき、価格だけでなく「築年数・最寄り駅・設備・推しポイント・周辺施設」も縦に並べたかったので、比較表タブを多面化。

- **画像解析（analyze API）の抽出項目を拡張：**
  - 築年数（`buildingAge`：例 "築15年" / "新築"）
  - 最寄り駅（`nearestStation`）と徒歩分数（`stationWalkMinutes`）
  - 設備配列（`facilities`：オートロック・宅配BOX・バストイレ別・追い焚き・独立洗面台・エアコン・室内洗濯機置場・モニター付インターホン・2階以上・南向き・駐車場あり・ペット可・楽器可。画像にあるものだけ配列で返す。リスト外も自由追加可）
  - 画像から読めなかったときは空文字／0／空配列でフォールバック
- **手入力欄を追加（見積もり編集画面）：**
  - 「物件詳細（比較表に並べる項目）」セクションを新設
  - 築年数・最寄り駅・徒歩分数のテキスト/数値欄
  - 設備チェックボックス（候補12種＋自由追加）
  - **おすすめポイント** の手入力 textarea（1〜2行）
  - PDFには反映しない（`no-print`）。比較表用専用
- **周辺施設は AI担当者コメント生成時のデータを流用（二重課金回避）：**
  - `/api/generate-comment` のレスポンスに `nearby` オブジェクトを追加
  - クライアント側で受け取った `nearby` を state に保持し、見積もり保存時に `SavedEstimate.nearby` として localStorage に格納
  - 比較表表示時は Google Places を再呼び出ししない
  - コメント未生成の物件は比較表で「周辺施設」セクションが「—」表示
- **比較表（CompareTab）に行を追加：**
  - 物件情報セクション：築年数・最寄り駅（徒歩分数付き）・設備・おすすめポイント
  - 周辺施設セクション（少なくとも1物件にnearbyデータがある場合のみ表示）：スーパー・コンビニ・ドラッグストア・公園・小学校（各代表1件）
  - 設備とおすすめポイントは1セルが縦に伸びるよう `align-top` + `whitespace-pre-wrap`
  - スマホは既存の `overflow-x-auto` で横スクロール
- **古いデータとの互換性：**
  - `ExtractedProperty` の新フィールドは全て optional
  - `SavedEstimate.nearby` も optional
  - 全て `?? "—"` / `?? 0` / `?? []` でフォールバック → 既存の localStorage データ・Supabase 共有URLは壊れない
- **型の一元化：**
  - `NearbyPlace` / `NearbyResult` を `src/types/index.ts` に移し、`generate-comment` API と `CompareTab` で共通利用

**変更ファイル：**
- 更新：`src/types/index.ts`（ExtractedProperty に5フィールド追加、SavedEstimate に nearby 追加、NearbyPlace/NearbyResult を export）
- 更新：`src/app/api/analyze/route.ts`（プロンプトに築年数・駅・設備を追加、JSONマッピングも対応）
- 更新：`src/app/api/generate-comment/route.ts`（型を共通化、レスポンスに nearby を含める）
- 更新：`src/app/page.tsx`（nearby state・手入力UIセクション追加・保存/復元/リセット時の処理）
- 更新：`src/components/CompareTab.tsx`（新行・新セクション・CompRow に wrap オプション追加）

**SQL変更：**
- **不要**。比較表は localStorage 保存、Supabase `estimates` は `data` JSONB に丸ごと格納のため新キー追加だけでスキーマALTERは不要。
- housing-jp の `SELECT *` 連携も非破壊（既存キーは触っていない・新キーは optional）。

**注意点：**
- Google Places の呼び出しは「コメント生成時のみ」になったため、コメント生成しないと比較表の「周辺施設」セクションは出ない（駅徒歩分数は手入力／AI抽出で埋められる）
- 設備の自由入力ぶんは画面下に削除ボタン付きチップで表示

---

### 2026-05-26（午後・改善）

**担当者コメントAI生成を「生活のリアルなイメージ」が伝わる内容に改善**

顧客視点で「ここで本当に生活できるか」が一番の不安、という前提に立って、不動産説明ではなく生活感が伝わるコメントを生成するように改修。

- **プロンプトの文言整理：** 「外国人」「はじめて日本で」などの言い回しを完全に削除。「日本で暮らす一般のお客様」として書く方針に変更
- **Google Places APIで取得する施設を大幅に拡充：**
  - 交通：駅（最寄り2件、徒歩分数つき）、バス停（駅が遠い場合のみコメントで使用）
  - 買い物：スーパー、コンビニ、ドラッグストア、100円ショップ
  - 医療：内科クリニック、歯科クリニック
  - 子育て：公園、保育園、幼稚園、小学校
  - その他：コインランドリー、郵便局、銀行ATM
  - 徒歩分数は物件と施設の直線距離から「80m/分」で算出（Google目安）
- **housing-jpのLINEヒアリング結果と連携：**
  - URLパラメータ `?customerId=xxx` で物件ページを開くと、Supabase `customers` テーブルから顧客情報を取得してパーソナライズ
  - 顧客情報全体をJSONでClaudeに渡し、加えてキーワード判定で「強調すべきポイント」を抽出（駐車場・子育て・ペット・自転車・駅遠など）
  - LINE連携は任意。`customerId` がなくても通常通り生成できる
- **コメントの構成を3〜4文に明確化：**
  - 1文目：物件の一番のセールスポイント
  - 2文目：交通アクセス（駅徒歩分数 or バス情報）
  - 3文目：生活施設（顧客情報があれば強調を変える）
  - 4文目：担当者としての一言（押し付けがましくない）
- **画面の変更：** 「AIで下書きを生成」エリアに `?customerId=xxx` が付いている場合「顧客連携: xxxxxxxx…」バッジを表示
- **新規環境変数：** なし（既存の `NEXT_PUBLIC_SUPABASE_URL` と `SUPABASE_SERVICE_ROLE_KEY` を流用）

**変更ファイル：**
- 更新：`src/app/api/generate-comment/route.ts`（施設拡充・顧客連携・プロンプト書き換え）
- 更新：`src/app/page.tsx`（URLパラメータ取得・顧客連携バッジ・APIへcustomerId送信）

**注意点：**
- housing-jpの `customers` テーブルの列名は `SELECT *` 方式で取り出してJSONそのままClaudeに渡しているため、列名が変わっても基本動作する設計
- 仮にテーブル名が `customers` 以外（例：`leads`、`users` など）になっている場合は `route.ts` の `fetchCustomer` 関数内のテーブル名を変更する必要あり
- Places APIの呼び出し回数が15種類になり、1回の生成あたり約$0.48かかる見込み（B2Bツールとして許容範囲）

---

### 2026-05-26（午前）

**担当者コメントAI自動生成機能を追加**

- 担当者コメント欄に「AIで自動生成」ボタンと言語選択プルダウン（10言語）を追加
- 新APIエンドポイント `/api/generate-comment` を作成
  - 入力：物件情報・費用情報・選択言語
  - Google Places API（Geocoding + Nearby Search）で物件住所から周辺施設（駅・スーパー・コンビニ・学校）を取得
  - Claude API（claude-sonnet-4-6）で「日本語3〜4文＋選択言語3〜4文」の併記コメントを生成
  - 生成結果はtextareaに入力され、そのまま手動編集可能
- Vercel環境変数 `GOOGLE_PLACES_API_KEY` を追加（未設定時は周辺施設なしで動作）
- 物件スペックの「築年数・設備」は今回はスキップ。抽出済み情報（間取り・面積・家賃等）と周辺施設のみで生成
- housing-jpのヒアリング結果との連携は今回見送り（後回し）

**変更ファイル：**
- 新規：`src/app/api/generate-comment/route.ts`
- 更新：`src/app/page.tsx`（担当者コメント欄UI改修、handleGenerateComment追加）

**Google Cloud側で必要な作業（山田さん側）：**
1. Google Cloud Console で新規プロジェクトまたは既存プロジェクトを選択
2. 「APIとサービス」→「ライブラリ」で以下を有効化：
   - Geocoding API
   - Places API
3. 「APIとサービス」→「認証情報」→「認証情報を作成」→「APIキー」
4. 作成したキーを Vercel の環境変数 `GOOGLE_PLACES_API_KEY` に設定
5. OneDrive「アカウント＆APIキー一覧.txt」にもキーをメモ

---

## Claude Code への指示文テンプレート

```
realpro（見積書アプリ）の続きをお願いします。
プロジェクト内の「realpro_マスタードキュメント_2026-05-26.md」を読んでから作業を始めてください。

今日やりたいこと：
（Phase 1のタスクから選ぶか自由記述）
```

---

*このドキュメントは随時更新する。作業のたびにこのファイルも更新すること。*
